[package]
name = "dex-indexer"
version = "0.1.0"
edition = "2024"

[dependencies]
ethers = "2.0"
tokio = { version = "1", features = ["full"] }
dotenv = "0.15"
futures-util = "0.3"
eyre = "0.6"
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres", "macros"] }

